apiVersion: v1
kind: Service
metadata:
  name: {{ include "istio-sidecar-with-gatewayapi.fullname" . }}
  labels:
    {{- include "istio-sidecar-with-gatewayapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "istio-sidecar-with-gatewayapi.selectorLabels" . | nindent 4 }}
