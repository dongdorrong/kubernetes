{{- if .Values.destinationRule.enabled }}
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: {{ include "istio-ambient-with-gatewayapi.fullname" . }}
  labels:
    {{- include "istio-ambient-with-gatewayapi.labels" . | nindent 4 }}
spec:
  host: {{ default (include "istio-ambient-with-gatewayapi.fullname" .) .Values.destinationRule.host }}
  {{- with .Values.destinationRule.trafficPolicy }}
  trafficPolicy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.destinationRule.subsets }}
  subsets:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
